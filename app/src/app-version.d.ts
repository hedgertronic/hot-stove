/** Build-time constant injected by Vite `define` (vite.config.ts) from
 * package.json's version — the bug-report body stamps it so an issue filed
 * against last month's deploy is distinguishable from today's. */
declare const __APP_VERSION__: string;
