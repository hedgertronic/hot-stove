[project]
name = "hot-stove"
version = "0.1.0"
description = "Data pipeline for the Hot Stove baseball drafting game"
requires-python = ">=3.12"
dependencies = ["fungo>=2.0.0"]

[tool.uv]
package = false
